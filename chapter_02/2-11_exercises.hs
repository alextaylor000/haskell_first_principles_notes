-- Parenthesization
-- 1.
a       = 2 + 2 * 3 - 1
aPretty = 2 + (2 * 3) - 1

-- 2.
b       = (^) 10 $ 1 + 1
bPretty = (^) 10 $ (1 + 1)

-- 3.
c       = 2 ^ 2 * 4 ^ 5 + 1
cPretty = ((2 ^ 2) * (4 ^ 5)) + 1


-- Equivalent expressions
-- 1. will return the same
-- 2. will not return the same
-- 3. will not return the same
-- 4. will not return the same
-- 5. will not return the same
